pipeline {
    agent { docker { image 'ubuntu:16.04' } }
    stages {
        stage('build') {
            steps {
                sh 'echo "The ubuntu docker image is running."'
            }
        }
    }
}

pipeline {
    agent { docker { image 'ubuntu:16.0.4' } }
    stages {
        stage('build') {
            steps {
                echo 'The ubuntu docker image is running.'
            }
        }
    }
}
